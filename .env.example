# ===== 智谱 GLM Coding Plan 配置 =====
# 设为 true 启用智谱，false 或不设置则使用原版 Claude
USE_ZHIPU=false

# 智谱 API Key（USE_ZHIPU=true 时必填）
# ZHIPU_API_KEY=your_zhipu_api_key_here

# 智谱服务端点（二选一）
# 国内版（默认）
# ZHIPU_BASE_URL=https://open.bigmodel.cn/api/anthropic
# 国际版 (Z.AI)
# ZHIPU_BASE_URL=https://api.z.ai/api/anthropic

# 可选：超时设置（毫秒）
# ZHIPU_TIMEOUT_MS=300000

# ===== Slack 配置（可选）=====
# 如果不配置 Slack，daemon 将以 headless 模式运行
# Headless 模式下：
#   - 无实时通知，使用 'sle check' 或日志监控
#   - 任务仍会正常执行并记录
#   - 报告仍会自动生成
# SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# SLACK_APP_TOKEN=xapp-your-slack-app-token

# ===== Telegram 配置（可选，优先于 Slack）=====
# 使用 @BotFather 创建 bot 获取 token
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
#
# 可选：白名单（逗号分隔的 chat ID 列表）
# 如不设置，所有人都可使用 bot
# TELEGRAM_ALLOWED_CHAT_IDS=12345,67890

# Claude Code CLI (optional - defaults to 'claude' from PATH)
# CLAUDE_CODE_BINARY_PATH=/usr/local/bin/claude

# Agent Config
AGENT_WORKSPACE_ROOT=./workspace
AGENT_DB_PATH=./workspace/data/tasks.db
AGENT_RESULTS_PATH=./workspace/data/results

# Git
GIT_USER_NAME=Sleepless Agent
GIT_USER_EMAIL=agent@sleepless.local

# Optional
LOG_LEVEL=INFO
DEBUG=false
